'use strict';

/**
 * projects-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::projects-category.projects-category');
